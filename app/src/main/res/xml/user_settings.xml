<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">
    <!--Containing Edittext -->
    <PreferenceCategory android:title="Settings">
        <EditTextPreference
            android:key="prefUserPassword"
            android:summary="set Your password"
            android:title="password"></EditTextPreference>
    </PreferenceCategory>
    <!--Containing checkBox and list -->
    <PreferenceCategory android:title="Security Settings">
        <CheckBoxPreference
            android:defaultValue="false"
            android:key="PrefLoackScreen"
            android:summary="Lock the Screen with Password"
            android:title="Lock Screen" />
        <ListPreference
            android:entries="@array/listArray"
            android:entryValues="@array/listArray"
            android:key="prefUpdateFrequency"
            android:summary="Set update frequency timer"
            android:title="Remider for updation" />
    </PreferenceCategory>
</PreferenceScreen>

